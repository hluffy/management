alter table 表名  add 字段名 字段类型
alter table 表名   alter column 字段名 set  (not) null
alter table 表名   add constratint fk foreign key (字段名) references 表名(字段名)

create table binding(
	id integer generated by default as identity,
	frame_num varchar(200) not null,
	equipment_num varchar(200) not null,
	bind_time Timestamp,
	unbind_time Timestamp,
	primary key (id),
	unique key (equipment_num,frame_num)
)

alter table 表名   add unique (字段名1，字段名2)